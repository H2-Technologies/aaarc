name: Deploy to Server

on:
  push:
    branches:
      - "main"
  workflow_dispatch:

jobs:
  build_api:
    runs-on: self-hosted
    steps:
      - name: Setup
        uses: actions/checkout@v4
      - name: Deploy
        run: docker compose up --build -d
